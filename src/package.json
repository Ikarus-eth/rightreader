{
  "name": "right-reader-build",
  "private": true,
  "scripts": {
    "build": "esbuild entry.jsx --bundle --minify --outfile=../app.js --loader:.jsx=jsx --loader:.json=json --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
